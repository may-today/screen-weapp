<view class="flex flex-col h-full overflow-hidden">
  <view class="flex flex-row items-center gap-3 h-12 border-b border-border px-4">
    <text class="font-semibold text-sm">{{ title }}</text>
    <slot name="header" />
  </view>
  <view class="flex-1 flex flex-col overflow-hidden">
    <slot />
  </view>
</view>