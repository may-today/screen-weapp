<view class="flex flex-col px-4 pb-8 gap-3">
  <device-list-item
    wx:for="{{list}}"
    wx:key="deviceId"
    wx:for-item="item"
    device="{{item}}"
    bind:select="handleSelectItem"
  />
</view>
<connect-panel device="{{connectingDevice}}" />